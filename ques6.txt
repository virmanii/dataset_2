# Objective
Using the Census of India 1951 Appendices to the Census Report (Volume I, Part I-B / Madras State Abstract), answer the following.

# Task
Across all the individual Divisions listed, what is the lowest ratio of Employers (Persons) to Employees (Persons) that any Division gets? Identify that Division and compute its Employers (Persons) divided by its Employees (Persons), rounded to six decimal places.

# Output Formatting & Constraints
* Format: Output a strictly valid JSON object inside a markdown code block.
* Required Keys: Return the JSON using EXACTLY the following four keys:
  - "Final_Answer" (Number): The definitive numeric value of the Employers/Employees ratio for the identified Division, strictly rounded to six decimal places.
  - "Evidence_Text" (String): A concise synthesis specifying the identified Division's name and the exact source locations, document, page, row, and column, for both values retrieved.
  - "Derivation" (String): The explicit step-by-step methodology used to identify the Division and compute the final result.
  - "Reference" (Array of Arrays): Formatted precisely as [[Document_Name, PDF_Page_Number]]. The page number MUST be the page's physical position within the PDF file (counting the first page of the file as page 1), NOT any page number printed on the page itself.
* Data Rigor: Do not invent figures or pull from external parametric knowledge. No other response apart from the suggested JSON is valid.


{
  "Final_Answer": 0.009772,
  "Evidence_Text": "Comparing Employers (Persons) against Employees (Persons) across all Division-level 'Total' rows, the lowest ratio belongs to Division 8 (Health, education and public administration), with Employers = 2,958 and Employees = 302,709.",
  "Derivation": "Step 1, for each of the ten major Divisions (0 through 9), take the 'Total' row (excluding Rural/Urban sub-rows and any nested Sub-division rows) and compute Employers (Persons) divided by Employees (Persons). Step 2, the minimum value belongs to Division 8: 2,958 / 302,709 = 0.00977176. Step 3, round to six decimal places: 0.009772.",
  "Reference": [["42078_1951_CEN.pdf", 33]]
}

{
  "criterion 1": {
    "description": "Final answer is exactly 0.009772.",
    "criterion_type": ["Math"]
  },
  "criterion 2": {
    "description": "Correctly identifies Division 8 (Health, education and public administration) as the Division with the lowest Employers/Employees ratio.",
    "criterion_type": ["Table Understanding", "Reasoning"]
  },
  "criterion 3": {
    "description": "Distinguishes Division-level Total rows from their Rural/Urban sub-rows and from nested Sub-division rows in the table's hierarchy, comparing only across the ten major Divisions rather than including Rural/Urban splits or finer sub-divisions.",
    "criterion_type": ["Table Understanding", "Reasoning"]
  },
  "criterion 4": {
    "description": "Extracts the correct Employers (Persons) value (2,958) and Employees (Persons) value (302,709) specifically from Division 8's own Total row, not from a Rural/Urban row or a different Division.",
    "criterion_type": ["Extraction (recall)"]
  },
  "criterion 5": {
    "description": "Computes the ratio as Employers divided by Employees, not the inverted ratio (Employees divided by Employers).",
    "criterion_type": ["Reasoning", "Math"]
  },
  "criterion 6": {
    "description": "Correctly evaluates all ten Divisions (0-9) rather than stopping early or omitting a Division, so as not to miss the true minimum (e.g. Division 6 has a much higher ratio of ~0.500410 and should not be mistaken for the minimum).",
    "criterion_type": ["Reasoning", "Instruction Following"]
  },
  "criterion 7": {
    "description": "Reference array includes the PDF's physical page number (33), not the page number printed on the scanned page.",
    "criterion_type": ["Instruction Following"]
  }
}
